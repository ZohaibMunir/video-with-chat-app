{
  "name": "video_chat_and_messages",
  "version": "1.0.0",
  "description": "AN Application enable a chat between its members",
  "main": "server.js",
  "scripts": {
    "start": " node server.js",
    "start-dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "compression": "^1.7.4",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "socket.io": "^2.3.0"
  },
  "engines": {
    "node": ">=10.0.0"
  }
}
